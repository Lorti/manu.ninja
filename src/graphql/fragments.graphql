fragment frontmatterFragment on MarkdownRemark {
  frontmatter {
    title
    htmlTitle
    path
    date(formatString: "MMM DD, YYYY")
    categories
    tags
    summary
    thumbnail
    external
    language
    sharing
  }
}
